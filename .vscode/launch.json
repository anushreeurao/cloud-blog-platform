{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Next.js: dev + Chrome (localhost)",
      "type": "node-terminal",
      "request": "launch",
      "command": "npm run dev",
      "serverReadyAction": {
        "action": "debugWithChrome",
        "pattern": "- Local:.+(https?://.+)",
        "uriFormat": "http://localhost:3000",
        "webRoot": "${workspaceFolder}"
      }
    }
  ]
}
